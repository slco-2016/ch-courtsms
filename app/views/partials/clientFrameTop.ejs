<% let clientBaseURL = `${level === "org" ? "/org" : ""}/clients/${client.clid}`; %>

<% include ./navBarTop %>

<!-- Load in addition CSS -->
<link rel="stylesheet" type="text/css" href="/static/css/v4_hubFrame.css">

  <div class="navSub">
    <div class="allLeftParent">

      <div class="allLeft">

        <div class="clientTab <%= hub.tab == 'profile' ? 'open' : 'closed' %>">
          <div class="icon">
            <a href="<%=`${clientBaseURL}`%>/edit">
              <i class="fa fa-cog" aria-hidden="true"></i>
            </a>
          </div>
          <div class="title">
            <a href="<%=`${clientBaseURL}`%>"><%= client.first + ' ' + client.last %></a>
          </div>
        </div>
        <%- leftTab('messages', hub, level, ['all', 'texts', 'emails'], 'messages', null, client.clid, 'method') %>
        <%- leftTab('notifications', hub, level, ['pending', 'sent'], 'notifications', null, client.clid, 'status') %>
        <%- leftTab('contact methods', hub, level, null, 'communications', null, client.clid, 'status') %>
      </div>
    </div>


    <div class="allRightParent">
      <div class="allRight">
        <%- rightTab('clients', 'mobile', level, 'contact method', `${client.clid}/communications/create`) %>
        <%- rightTab('notifications', 'bell', level, 'notification', `create?client=${client.clid}&user=${client.cm}`) %> 
        <%- rightTab('clients', 'comments', level, 'message', `${client.clid}/address`) %>
      </div>
    </div>

  </div>

  <div class="coreContent">